apiVersion: v1
kind: Service
metadata:
  name: react-app-service
  namespace: react-app-ns
spec:
  selector:
    app: react-app
  type: LoadBalancer
  ports:
  - port:  80
    protocol: TCP
    targetPort: 3000