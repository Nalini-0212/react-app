apiVersion: v1
kind: Service
metadata:
  name: react-app-service
  namespace: react-app-ns
spec:
  selector:
    app: react-app
  type: LoadBalancer
  ports:
  - port:  3000
    protocol: TCP
    targetPort: 80